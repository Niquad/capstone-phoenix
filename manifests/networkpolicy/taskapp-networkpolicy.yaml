apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: taskapp-network-policy
  namespace: taskapp
spec:
  podSelector: {}

  policyTypes:
    - Ingress
    - Egress

  ingress:
    - from:
        - podSelector: {}

  egress:
    - to:
        - podSelector: {}